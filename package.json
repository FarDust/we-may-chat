{
  "name": "chat-room-heroku-dummy",
  "version": "0.1.0",
  "scripts": {
    "heroku-postbuild": "npm install",
    "postinstall": "cd frontend/chat-room && export NPM_CONFIG_PRODUCTION=false && npm install && npm run build-local"
  },
  "private": true,
  "dependencies": {
    "@angular/cli": "^11.2.9"
  },
  "devDependencies": {}
}
